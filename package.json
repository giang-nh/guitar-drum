{
  "name": "guitar-drum",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "playwright test -c playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
